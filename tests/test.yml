---
- hosts: localhost
  connection: local

  vars_files:
    - ../vars/zsh.yml
    - ../vars/rvm.yml

  tasks:
    - include: ../tasks/ansible.yml
    - include: ../tasks/zsh.yml

  roles:
    - rvm_io.ruby