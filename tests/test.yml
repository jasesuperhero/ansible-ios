---
- hosts: localhost
  connection: local

  vars_files:
    - ../vars/zsh.yml
    - ../vars/homebrew.yml

  tasks:
    - include: ../tasks/ansible.yml
    - include: ../tasks/zsh.yml

  roles:
    - geerlingguy.homebrew
